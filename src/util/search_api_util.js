// import { createAsyncThunk } from "@reduxjs/toolkit";
// import axios from "axios";
// import { TEMP_CONFIG } from '../util/credentials_api_testing'

// export const fetchSearchRecomendations = createAsyncThunk('search/receiveSearchRecomendations', async () => {

// })

// This might not be needed, the only thing that we might need is 
// a search slice